# Vega CLI 设计思路

## 定位

CLI 是给 AI 用的接口，不是给人类的。人类直接操作 Markdown 文件。

交互模型：

```
人类 ←→ Markdown 文件 ←→ AI（通过 CLI/MCP）
```

## 安装与分发

使用 `uv` 作为分发工具，`uv tool install` 安装后全局可用。

## 接口设计原则

- 输出 JSON 格式，方便 AI 解析
- 命令应该简洁直观，AI 看命令名就能理解用途
- 需要覆盖的基本操作：检索、读取、写入、删除、索引维护
